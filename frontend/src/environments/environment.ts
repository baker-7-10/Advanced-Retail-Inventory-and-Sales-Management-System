export const environment = {
  production: false,
  /** Base REST URL for the NestJS backend */
  apiUrl: "http://localhost:3000/api/v1",
  /** Socket.IO gateway origin (no /api/v1 prefix) */
  socketUrl: "http://localhost:3000",
};
